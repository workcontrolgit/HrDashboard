{
  "Logging": { "LogLevel": { "Default": "Information" } },
  "Database": {
    "Provider": "Oracle",
    "VisibleTables": "EMPLOYEES,DEPARTMENTS,JOBS,LOCATIONS"
  },
  // SECURITY NOTE: the Oracle "hr" user above is schema-scoped to the HR tables only.
  // If ConnectionStrings:HrData is ever pointed at a SqlServer instance instead, do NOT
  // reuse a Trusted_Connection/sysadmin identity (e.g. LocalDB's default developer identity) —
  // create a SQL Server login scoped to SELECT-only on EMPLOYEES/DEPARTMENTS/JOBS/LOCATIONS
  // first, matching this Oracle user's security posture, before pointing this at any
  // non-throwaway SQL Server instance.
  "ConnectionStrings": {
    "HrData": "User Id=hr;Password=HrUser_2026;Data Source=localhost:1521/XEPDB1;"
  },
  "Urls": "http://localhost:5200"
}
